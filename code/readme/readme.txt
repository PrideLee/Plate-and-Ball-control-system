/*
摄像头采样率 15fps
1、还有摄像头未测试，按键不全，其它都已通过测试，摄像头等线接好再测                                               （此行失效）
2、Timer20msInit();  已改为20ms  因使用_Test_(); 的缘故，此函数中关于FreeRTOS的东西被注释掉了，后面需要取消注释


测试摄像头、舵机信号（后期仍需测试信号范围（未测试））、6按键、俩串口、俩波形输出      （可用）
坐标未确定（显示屏的坐标与摄像头坐标一致）
调零程序未写
		（屏上画个方框，如（x,y）-（x1,y1）,对应球板60*60，算出比例即可将摄像头坐标转为球板坐标，再显示屏定出四角四个点用以摆正球板）
完成后几问
*/

英文显示
	LCD_Display_Dir(0);
	LCD_AUTOScan_Dir(L2R_U2D);
	
摄像头显示
	LCD_Display_Dir(1);
	LCD_AUTOScan_Dir(L2R_U2D);